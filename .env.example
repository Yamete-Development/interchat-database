# Database connection URL
DATABASE_URL=postgresql+asyncpg://user:password@localhost:5432/database

# Debug mode (enables verbose logging)
DEBUG=False

# Production mode (disables rich logging)
PRODUCTION=False
