[project]
name = "db"
version = "1.0.0"
description = "Generic database connection and session management for SQLAlchemy"
requires-python = ">=3.14"
dependencies = ["sqlalchemy", "rich", "asyncpg", "python-dotenv"]

[build-system]
requires = ["uv_build>=0.8.15,<0.9.0"]
build-backend = "uv_build"
